export interface SmzDocumentConfig {
  titles: {
    container: string;
    text: string;
  };
  dividers: {
    styles: string;
  };
  fields: {
    container: string;
    label: string;
    text: string;
  };
}